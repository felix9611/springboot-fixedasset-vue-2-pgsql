<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.fixedasset.mapper.CodeTypeMapper">
    <select id="getALL" resultType="com.fixedasset.entity.CodeType">
        SELECT * FROM code_type WHERE type=#{typeCode} AND statu = 1
    </select>
</mapper>